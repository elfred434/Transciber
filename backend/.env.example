# Android transcribes and summarizes locally; this gateway receives text only for translation.
# Never commit a real Gemini key. Store it in Secret Manager in production.
GEMINI_API_KEY=replace-me
GEMINI_TRANSLATION_MODEL=gemini-3.1-flash-lite
PORT=8787
# Optional shared secret for the Android client.
GATEWAY_CLIENT_TOKEN=replace-me
